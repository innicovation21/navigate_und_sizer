name: packages_navigation_and_images
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

# in den meisten Fällen, werden Packages die wir benutzen wollen unter den dependencies eingefügt
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  # importieren des Packages Sizer in unser Projekt
  sizer: ^2.0.15 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  
